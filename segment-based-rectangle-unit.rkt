#lang racket

(require "rectangle-sig.rkt")

(define-unit segment-based-rectangle@
  (import)
  (export rectangle^)
  
  (define (make-rect ...))
  (define (area ...))
  (define (perim ...)))

(provide segment-based-rectangle@)